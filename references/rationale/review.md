# Review rationale

An independently updated helper can change its default model without a pack change. Explicit routing keeps the review tied to the approved policy.

A weaker reviewer tends to miss the producer's difficult errors. Cross-vendor independence addresses shared priors in subjective product judgment; code review instead has a concrete diff and contracts to inspect. Same-model review shares blind spots, which explains the retained live-QA and test-integrity safeguards.
